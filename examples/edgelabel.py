import bmt

b = bmt.is_edgelabel('negatively regulates, process to process')

print(b)

b = bmt.is_edgelabel('negatively regulates')

print(b)

b = bmt.is_edgelabel('positively regulates, entity to entity')

print(b)

